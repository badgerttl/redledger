# Penetration Test Report — {{ engagement.name }}

**Client:** {{ engagement.client_name or 'N/A' }}
**Contact:** {{ engagement.client_contact or 'N/A' }}
**Date:** {{ engagement.start_date or 'N/A' }} — {{ engagement.end_date or 'N/A' }}
**Status:** {{ engagement.status }}

---

## Executive Summary

This penetration test engagement identified **{{ findings|length }}** finding(s) across the following severity levels:

| Severity | Count |
|----------|-------|
{% for sev in ['Critical', 'High', 'Medium', 'Low', 'Info'] %}{% if sev in severity_counts %}| {{ sev }} | {{ severity_counts[sev] }} |
{% endif %}{% endfor %}

Methodology completion: **{{ checked_checklist }}/{{ total_checklist }}** checklist items completed.

{% if severity_counts.get('Critical', 0) > 0 %}**URGENT:** {{ severity_counts['Critical'] }} critical finding(s) require immediate attention.{% endif %}

---

## Scope

### In Scope
{{ scope.in_scope if scope and scope.in_scope else 'Not defined.' }}

### Out of Scope
{{ scope.out_scope if scope and scope.out_scope else 'Not defined.' }}

{% if scope_entries %}### Scope Entries
| Type | Value |
|------|-------|
{% for e in scope_entries %}| {{ e.entry_type }} | {{ e.value }} |
{% endfor %}{% endif %}

---

## Findings

{% for f in findings %}### {{ loop.index }}. [{{ f.severity }}] {{ f.title }}

| Property | Value |
|----------|-------|
| Severity | {{ f.severity }} |
| CVSS Score | {{ f.cvss_score if f.cvss_score is not none else 'N/A' }} |
| CVSS Vector | {{ f.cvss_vector or 'N/A' }} |
| Status | {{ f.status }} |
| Phase | {{ f.phase or 'N/A' }} |

#### Description
{{ f.description or 'No description provided.' }}

#### Impact
{{ f.impact or 'No impact statement provided.' }}

#### Remediation
{{ f.remediation or 'No remediation provided.' }}

{% if f.affected_assets %}**Affected Assets:** {% for a in f.affected_assets %}{{ a.name }} ({{ a.target }}){% if not loop.last %}, {% endif %}{% endfor %}{% endif %}

---

{% endfor %}

## Rules of Engagement

{{ engagement.rules_of_engagement or 'Not specified.' }}

---

*Report generated by RedLedger*
